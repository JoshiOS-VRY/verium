{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "ios",
  "description": "iOS-only permissions (Face ID / Touch ID unlock).",
  "windows": ["main"],
  "permissions": ["biometric:default", "notification:default"]
}
