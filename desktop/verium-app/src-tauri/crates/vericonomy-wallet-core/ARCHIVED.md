# Archived: local `vericonomy-wallet-core`

This directory previously held a duplicate of `vericonomy-wallet-core`. Source has been removed.

The canonical crate lives at:

```
vericonomy-sdk/vericonomy-wallet-core/
```

The Tauri app depends on the workspace via `Cargo.toml` path deps to `../../../../vericonomy-sdk/vericonomy-wallet-core`.
