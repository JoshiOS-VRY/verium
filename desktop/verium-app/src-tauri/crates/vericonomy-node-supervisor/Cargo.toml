[package]
name = "vericonomy-node-supervisor"
version = "1.0.0-alpha.1"
description = "Pure node lifecycle state machine, process registry, and retry guards for the Vericonomy wallet."
authors = ["Vericonomy developers"]
edition = "2021"
rust-version = "1.77"

[lib]
name = "vericonomy_node_supervisor"

[dependencies]
serde = { version = "1", features = ["derive"] }
